{
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build": "npm --prefix ./sso-tile-app run build && npm --prefix ./assets run build"
    },
    "dependencies": {
        "express": "^4.18.1",
        "@aws-sdk/client-s3": "^3.0.0",
        "@aws-sdk/lib-storage": "^3.0.0",
        "multer": "^1.4.5-lts.1",
        "cors": "^2.8.5",
        "dotenv": "^16.0.1",
        "react": "^17.0.2",
        "react-dom": "^17.0.2",
        "react-scripts": "^5.0.1"
    },
    "devDependencies": {
        "nodemon": "^2.0.22"
    }
}
